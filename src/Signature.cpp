#include "Signature.h"

#include <utility>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

namespace cryptobox
{
    Signature::Signature(Buffer buffer)
    : r_buffer{std::move(buffer)}
    {

    }

    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

    const Buffer& Signature::getBuffer() const
    {
        return r_buffer;
    }
}
